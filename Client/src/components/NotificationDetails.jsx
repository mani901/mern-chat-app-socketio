import React from "react";

const NotificationDetails = ({ message }) => {
  return (
    <div>
      <h1>notificationDetails</h1>
      <p>{message}</p>
    </div>
  );
};

export default NotificationDetails;
